const test=require('node:test'); const assert=require('node:assert/strict'); const {buildKstartupUrl,buildManagerUrl,parseKstartup,parseManagers}=require('../lib/policy-sources');
test('API 키와 페이지 조건을 요청 주소에 넣는다',()=>{const a=new URL(buildKstartupUrl('decoded-key',{page:2,perPage:30})); assert.equal(a.searchParams.get('serviceKey'),'decoded-key'); assert.equal(a.searchParams.get('page'),'2'); const b=new URL(buildManagerUrl('key')); assert.match(b.pathname,/3060708\/v1\/uddi:/);});
test('K-Startup 공고 응답을 화면용 필드로 바꾼다',()=>{const p=parseKstartup({response:{header:{resultCode:'00'},body:{totalCount:1,items:{item:{pbanc_sn:'10',biz_pbanc_nm:'투자 연계 지원사업',pbanc_rcpt_end_dt:'20260930',detl_pg_url:'https://example.test/10'}}}}}); assert.equal(p.total,1); assert.equal(p.items[0].title,'투자 연계 지원사업'); assert.equal(p.items[0].end_date,'20260930');});
test('KVIC 자동변환 API의 한글 열을 정규화한다',()=>{const p=parseManagers({totalCount:1,data:[{대표운영사:'테스트벤처스',운영사구분:'창투사','자조합 규모(백만원)':'12,300'}]}); assert.deepEqual(p.items[0],{manager:'테스트벤처스',manager_type:'창투사',fund_size_million_krw:12300});});
